create table POST
(
    ID   BIGINT GENERATED BY DEFAULT AS IDENTITY,
    NAME VARCHAR(255),
    TITLE VARCHAR(500),
    CONTENTS VARCHAR(2000),
    CREATED_DATE TIMESTAMP NOT NULL,
    MODIFIED_DATE TIMESTAMP NOT NULL,
    PRIMARY KEY (ID)
);

INSERT INTO POST(NAME, TITLE, CONTENTS, CREATED_DATE) VALUES ('kim', '이거 되나요', '내용입니당',sysdate);
ALTER TABLE POST ADD MODIFIED_DATE TIMESTAMP;

